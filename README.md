
2048 in Quamolit
----

Trying to prove that Quamolit is powerful enough...

Demo(requires experimental Canvas fearures) http://repo.quamolit.org/2048

The Calcit 0.18.1 migration currently pins Quamolit `0.0.18-alpha.1` and uses
a compile-only bootstrap entry. CI verifies compilation, not gameplay; the
original app entry still needs strict-type migration before it can be restored.

### License

MIT
